# WatchParty 放映室 — 多人同步看番 + 语音聊天

## 功能概览

- **多人同步视频播放** — 播放/暂停/跳转自动同步给所有人
- **WebRTC 语音通话** — 基于浏览器的 P2P 语音聊天，无需安装任何软件
- **实时文字聊天** — 房间内文字消息
- **房间管理** — 创建/加入/离开房间，第一个进入者为房主
- **自动房主转移** — 房主离开后自动转移给下一个人

## 技术架构

```
┌──────────────┐    WebSocket     ┌──────────────────┐
│  浏览器 A     │ ◄──────────────► │  FastAPI 后端      │
│  (React)     │                  │  (WebSocket 信令)   │
└──────┬───────┘                  └──────────────────┘
       │                                    ▲
       │ WebRTC P2P (语音)                   │ WebSocket
       │                                    │
┌──────┴───────┐                  ┌─────────┴────────┐
│  浏览器 B     │ ◄──────────────► │  浏览器 C         │
│  (React)     │   WebRTC P2P    │  (React)          │
└──────────────┘                  └──────────────────┘
```

- **视频同步**: WebSocket 广播播放状态（play/pause/seek/time_update）
- **语音通话**: WebRTC P2P，后端仅做信令中转（offer/answer/ICE candidate）
- **STUN 服务器**: 使用 Google 公共 STUN（stun:stun.l.google.com:19302）

## 使用方法

### 1. 启动后端
```bash
cd d:/work/anime
uvicorn app.main:app --reload --port 8000
```

### 2. 启动前端
```bash
cd d:/work/anime/frontend
npm run dev
```

### 3. 打开放映室
访问 http://localhost:3000/watchparty

### 4. 使用流程
1. 输入昵称
2. 创建房间 或 输入房间 ID 加入
3. 粘贴视频链接（支持 MP4 / WebM / m3u8）
4. 点击"开启语音"按钮启用语音通话（需要允许浏览器麦克风权限）
5. 分享房间 ID 给朋友，让他们加入

## 支持的视频格式

| 格式 | 说明 |
|------|------|
| MP4 / WebM | 直链视频文件 |
| m3u8 | HLS 流媒体（需要浏览器原生支持或后续集成 hls.js） |

## 注意事项

- 语音通话需要 **HTTPS** 或 **localhost** 环境（浏览器安全限制）
- 跨网络通话可能需要 TURN 服务器（当前仅配置了 STUN）
- 视频链接必须允许跨域访问（CORS），本地文件暂不支持
- 房间数据存储在内存中，服务器重启后清空

## 后续可扩展

- [ ] 集成 hls.js 支持更多流媒体格式
- [ ] 添加 TURN 服务器支持（coturn）用于复杂网络环境
- [ ] 支持从番剧库直接发起放映
- [ ] 视频弹幕功能
- [ ] 房间密码保护
- [ ] 屏幕共享
